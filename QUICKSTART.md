# ⚡ Быстрый старт TGBot

## 1️⃣ Запуск БЕЗ бэкенда (только фронт с тестовыми данными)

```bash
cd front
npm install
npm start
```

Откройте `http://localhost:4200` - работает с моковыми данными! 🎉

---

## 2️⃣ Запуск С бэкендом (полная версия)

### Шаг 1: Настройка бэкенда

```bash
cd back

# Создайте эти файлы (инструкции в BACKEND_SETUP.md):
# - schemas.py
# - places_repository.py
# - Обновите database.py
# - Обновите main.py

# Установите зависимости
pip install fastapi uvicorn sqlalchemy aiosqlite pydantic

# Запустите
python main.py
```

Бэкенд запустится на `http://localhost:8000` 🚀

### Шаг 2: Запуск фронтенда

```bash
cd front
npm install
npm start
```

Фронтенд на `http://localhost:4200` теперь загружает данные из API! ✨

---

## 3️⃣ Проверка работы

### Откройте в браузере:
- **Фронтенд**: http://localhost:4200
- **Бэкенд API Docs**: http://localhost:8000/docs
- **Проверка API**: http://localhost:8000/api/places

### Консоль браузера (F12):
Если увидите "используются тестовые данные" - бэкенд недоступен, но приложение работает!

---

## 🎯 Что дальше?

### Для бэкенда:
1. Скопируйте код из `back/BACKEND_SETUP.md`
2. Создайте нужные файлы
3. Запустите `python main.py`

### Для фронтенда:
- Всё уже настроено! ✅
- Работает с API и fallback на моковые данные
- Полная адаптивность и темная тема

### Для Telegram:
1. Соберите фронт: `cd front && npm run build`
2. Загрузите `dist/TGBot/browser/` на хостинг
3. Создайте бота через @BotFather
4. Настройте Web App URL

---

## 🐛 Частые проблемы

**Бэкенд не запускается?**
```bash
# Проверьте Python версию (нужен 3.11+)
python --version

# Переустановите зависимости
pip install --upgrade -r requirements.txt
```

**Фронтенд показывает ошибки?**
```bash
# Очистите кэш и переустановите
rm -rf node_modules package-lock.json
npm install
```

**CORS ошибки?**
- Убедитесь, что в `main.py` есть `CORSMiddleware`
- Проверьте `allow_origins=["*"]` в настройках CORS

---

## 📚 Полная документация

- 📖 [Основной README](README.md)
- 🔧 [Настройка бэкенда](back/BACKEND_SETUP.md)
- 🎨 [Настройка фронтенда](front/FRONTEND_SETUP.md)

---

## ✅ Чеклист готовности к деплою

### Бэкенд
- [ ] Все файлы созданы
- [ ] База данных инициализирована
- [ ] Тестовые данные загружены
- [ ] API отвечает на `/api/places`
- [ ] CORS настроен

### Фронтенд
- [ ] `npm install` выполнен
- [ ] `environment.ts` настроен
- [ ] Приложение открывается в браузере
- [ ] Данные загружаются
- [ ] Навигация работает

### Telegram
- [ ] Бот создан через @BotFather
- [ ] Web App URL настроен
- [ ] Приложение открывается в Telegram
- [ ] Темы переключаются корректно

---

**Готово к работе! 🎉**

Если что-то не работает - проверьте консоль браузера и логи сервера.

