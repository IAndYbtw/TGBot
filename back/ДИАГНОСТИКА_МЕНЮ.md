# 🔍 Диагностика проблем с меню API

## ❌ Ошибка: `{"detail":"Not Found"}`

### Шаг 1: Проверьте, что сервер запущен

Откройте в браузере:
- http://127.0.0.1:8000/docs
- Должна открыться страница Swagger UI

Если не открывается → **Сервер не запущен!**

---

### Шаг 2: Проверьте, что данные загружены

Откройте в браузере:
- http://127.0.0.1:8000/api/places

Должен вернуться JSON с 4 кафе:
```json
[
  {"id": 1, "name": "Пандасад", ...},
  {"id": 2, "name": "Пицца Хот", ...},
  ...
]
```

Если пустой массив → **Данные не загружены!**
Решение: Перезапустите сервер (Ctrl+C, потом `python main.py`)

---

### Шаг 3: Проверьте endpoint меню напрямую

Откройте в браузере:
- http://127.0.0.1:8000/api/places/1/menu

**Ожидаемый результат:**
```json
{
  "place_id": 1,
  "place_name": "Пандасад",
  "menu": [
    {
      "id": 1,
      "name": "Борщ",
      "description": "Традиционный русский суп",
      "price": 200.0
    },
    ...
  ]
}
```

**Если видите ошибку:**
- `404 Not Found` → Endpoint не зарегистрирован (перезапустите сервер)
- `Место не найдено` → Нет места с таким ID в базе
- Пустой массив `menu: []` → Меню нет, но endpoint работает

---

### Шаг 4: Автоматический тест

Запустите тестовый скрипт:
```bash
cd back
pip install requests  # если еще не установлен
python test_menu_api.py
```

Скрипт проверит все endpoints и покажет, что не работает.

---

### Шаг 5: Проверьте консоль браузера (F12)

Откройте фронтенд (http://localhost:4200), нажмите F12, перейдите на вкладку "Console".

При открытии деталей кафе вы должны увидеть:
```
Запрос меню: http://localhost:8000/api/places/1/menu
```

Если видите ошибку, скопируйте её полностью.

---

## 🔧 Частые проблемы

### Проблема 1: Сервер не перезагрузился

**Решение:**
1. Остановите сервер (Ctrl+C в терминале)
2. Запустите заново: `python main.py`
3. Убедитесь, что видите: "✅ Загружено 4 кафе и 9 блюд"

### Проблема 2: База данных пустая

**Решение:**
1. Удалите файл `tasks.db` в папке `back`
2. Перезапустите сервер
3. Данные загрузятся автоматически из `fill_sample_data.py`

### Проблема 3: Неправильный URL

**Проверьте в `front/src/environments/environment.ts`:**
```typescript
apiUrl: 'http://localhost:8000/api'
```

Если бэкенд на другом порту, измените URL.

### Проблема 4: CORS ошибка

**Проверьте в консоли браузера:**
- Ошибка типа "CORS policy" → Убедитесь, что CORS middleware включен в `main.py`

---

## ✅ Правильная последовательность запуска

1. **Запустите бэкенд:**
   ```bash
   cd back
   python main.py
   ```
   Должны увидеть: "✅ Загружено 4 кафе и 9 блюд"

2. **Проверьте API:**
   - Откройте http://127.0.0.1:8000/api/places/1/menu
   - Должен вернуться JSON с меню

3. **Запустите фронтенд:**
   ```bash
   cd front
   npm start
   ```

4. **Откройте приложение:**
   - http://localhost:4200
   - Кликните на любое кафе
   - Меню должно отобразиться

---

## 🧪 Тест через curl (Windows PowerShell)

```powershell
# Проверка списка мест
Invoke-WebRequest -Uri "http://127.0.0.1:8000/api/places" | Select-Object -ExpandProperty Content

# Проверка меню кафе #1
Invoke-WebRequest -Uri "http://127.0.0.1:8000/api/places/1/menu" | Select-Object -ExpandProperty Content
```

---

## 📞 Если ничего не помогло

1. Скопируйте полную ошибку из консоли браузера (F12 → Console)
2. Скопируйте вывод сервера при запуске
3. Проверьте, что все файлы сохранены:
   - `back/main.py` - должен содержать endpoint `/api/places/{place_id}/menu`
   - `back/fill_sample_data.py` - должен содержать данные меню
   - `front/src/app/services/places.service.ts` - должен содержать метод `getPlaceMenu`

---

**После исправления перезапустите и бэкенд, и фронтенд!** 🔄

