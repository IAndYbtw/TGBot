# 🎨 Настройка фронтенда для TGBot

## 📦 Установка зависимостей

```bash
cd front
npm install
```

---

## ⚙️ Настройка API

Откройте файл `src/app/services/places.service.ts` и укажите адрес вашего бэкенда:

```typescript
private apiUrl = 'http://localhost:8000/api/places';
```

Если ваш бэкенд на другом порту или домене, измените URL соответственно.

---

## 🚀 Запуск в режиме разработки

```bash
npm start
```

или

```bash
ng serve
```

Приложение будет доступно на `http://localhost:4200`

---

## 🏗️ Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/TGBot/browser/`

---

## 📱 Запуск в Telegram

1. Соберите фронтенд: `npm run build`
2. Разместите файлы из `dist/TGBot/browser/` на хостинге
3. Создайте бота в Telegram через @BotFather
4. Настройте Web App URL в боте
5. Откройте мини-приложение в Telegram

---

## ✨ Особенности дизайна

### Адаптивность
- 📱 Мобильные экраны (до 375px)
- 📱 Стандартные мобильные (375px - 768px)
- 💻 Планшеты (768px - 1024px)
- 🖥️ Десктоп (от 1024px)

### Темы
- 🌞 Светлая тема
- 🌙 Темная тема
- 🎨 Автоматическое определение темы Telegram

### Анимации
- ✨ Fade-in при загрузке страниц
- 🎯 Плавные переходы между состояниями
- 👆 Тактильная обратная связь при нажатиях
- 🔄 Анимация стрелок навигации

---

## 🔧 Режим работы без бэкенда

Фронтенд автоматически работает с моковыми данными, если бэкенд недоступен:

- При ошибке подключения к API используются тестовые данные
- В консоли браузера появится предупреждение
- Приложение продолжит работать в обычном режиме

---

## 📂 Структура проекта

```
front/src/app/
├── pages/
│   ├── place/                 # Список мест
│   │   └── place.ts
│   └── placedetails/          # Детали места
│       └── place-details.ts
├── services/
│   └── places.service.ts      # API сервис
├── app.component.ts           # Корневой компонент
├── app.config.ts              # Конфигурация
└── app.routes.ts              # Маршруты
```

---

## 🎨 Кастомизация

### Изменение цветов

Приложение использует Telegram цветовую схему:
- `--tg-theme-bg-color` - фон
- `--tg-theme-text-color` - текст
- `--tg-theme-button-color` - кнопки
- `--tg-theme-hint-color` - подсказки
- `--tg-theme-secondary-bg-color` - вторичный фон

### Изменение размеров

В `styles.css` есть CSS переменные:
```css
:root {
    --main-padding: 16px;
    --card-radius: 16px;
    --transition-speed: 0.3s;
}
```

---

## 🐛 Отладка

### Проверка подключения к API

Откройте консоль браузера (F12) и проверьте:
- Запросы в Network tab
- Ошибки в Console tab
- Предупреждения о fallback на тестовые данные

### Проверка Telegram WebApp

```javascript
// В консоли браузера
console.log(window.Telegram.WebApp);
```

---

## 📊 Технологии

- **Framework**: Angular 18 (Standalone Components)
- **Styling**: CSS with CSS Variables
- **HTTP**: HttpClient with RxJS
- **Router**: Angular Router
- **Telegram**: Telegram WebApp API

---

## 🎯 Дальнейшее развитие

Возможные улучшения:
- 🛒 Добавить корзину для заказов
- ⭐ Система рейтингов
- 📝 Отзывы пользователей
- 🔍 Поиск и фильтры
- 💳 Интеграция платежей
- 📍 Карта с локациями
- 📅 Время работы заведений
- 🔔 Уведомления о статусе заказа

