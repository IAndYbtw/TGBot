export const environment = {
    production: false,
    // ⚠️ ВНИМАНИЕ: localhost может вызывать запрос разрешения на доступ к локальной сети
    // Это нормально для разработки. В продакшене используйте реальный домен.
    apiUrl: 'http://localhost:8000/api'
};

